import { IResponseMessage } from "@/interface";

export const responseMessage: IResponseMessage = {
  SOMETHING_WENT_WRONG: "Something went wrong",
  INVALID_URL: "Invalid URL",
};

export { responseMessage as RES_MSG };
